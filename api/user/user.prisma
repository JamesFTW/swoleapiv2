
import { Post } from "../post/Post"
import { Profile } from "../profile/Profile"

model User {
  id      Int      @id @default(autoincrement())
  email   String   @unique
  name    String?
  posts   Post[]
  profile Profile?
}